{
  "name": "acme-extension",
  "version": "0.1.0",
  "description": "Sample Chrome extension showcasing the Payhook UpgradeButton",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "dependencies": {
    "@payhook/extension": "^0.2.1"
  },
  "devDependencies": {
    "esbuild": "^0.21.0"
  }
}
