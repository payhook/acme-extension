{
  "name": "acme-extension",
  "version": "0.1.0",
  "description": "Sample Chrome extension showcasing the Payhook UpgradeButton",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=26.3.0"
  },
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "dependencies": {
    "@payhook/extension": "^0.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
